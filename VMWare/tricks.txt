Run VMWare Play (Free Version) in the background
1. Go to C:\Users\[username]\AppData\Roaming\VMware

2. Add the following to file preferences.ini

pref.vmplayer.exit.vmAction = "disconnect"

3. Kill VMWare process throw task manager

4. Open the VMWare GUI to shut down the virtual machine
